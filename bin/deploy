#!/bin/bash

set -o errexit
set -o pipefail

. "$( dirname "${BASH_SOURCE[0]}" )"/nvst

cd "$NVST_ROOT"
git pull
bundle
bundle exec rake db:migrate assets:precompile
git tag `date +%Y%m%d-%H%M%S`
restart
