%h2 Schedule K &mdash; Partners' Distributive Share Items
%table
  %thead
    %tr
      %th Category
      %th Total
      - @schedule_k.users.each do |user|
        %th= user.email
  %tbody
    %tr
      %th Ordinary income
      %td= number_to_currency -@schedule_k.expenses.sum(&:amount)
    - @schedule_k.events.each do |category, events|
      %tr
        %th= category
        %td= number_to_currency events.sum(&:amount)
    %tr
      %th Short-term capital gain
      %td= number_to_currency @schedule_k.close_transactions.sum(&:realized_gain)
